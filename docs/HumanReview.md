# HumanReview — ruoyu.vocabulary

> 代码审查发现项，按优先级排列。已解决项必须移除。

## P0 — 必须修复

（无）

## P1 — 应尽快修复

（无）

## P2 — 代码卫生

### HR-04: 中文异常消息

- **问题**：多处异常消息使用中文
- **A** (推荐)：统一改为英文，中文仅保留用户可见的 DisplayNames
- **B**：保持中文，在规范中允许 HTTP 错误消息使用中文
- **C**：暂不处理，不影响功能
- **批复**：C — 代码规范类，不影响功能正确性

### HR-05: 单文件包含多个类型

- **问题**：多个文件一个文件定义多个类/接口
- **A** (推荐)：每个类型拆分到独立文件，遵循 C# 约定
- **B**：按功能分组，每组一个文件
- **C**：暂不处理，不影响功能
- **批复**：C — 代码卫生类，不影响功能正确性

### HR-06: GetRandomExceptAsync 全表排序性能差

- **问题**：VocabularyRepository.cs 使用 OrderBy(Guid.NewGuid()) 实现随机排序，导致全表排序
- **A** (推荐)：改用数据库原生 RANDOM() / random() 函数排序
- **B**：预计算随机池，从池中排除后取随机
- **C**：暂不处理，当前数据量小
- **批复**：C — 性能类，当前数据量小

### HR-07: 依赖具体类而非接口

- **问题**：多处直接依赖具体实现类而非接口，不利于测试和替换
- **A** (推荐)：提取接口，DI 注册时绑定接口到实现
- **B**：仅在需要 Mock 测试的类提取接口
- **C**：暂不处理，当前耦合度可接受
- **批复**：C — 架构改进类，当前可接受
